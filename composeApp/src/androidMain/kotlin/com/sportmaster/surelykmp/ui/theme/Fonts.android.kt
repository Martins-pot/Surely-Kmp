package com.sportmaster.surelykmp.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import com.sportmaster.surelykmp.R

actual val RushonGroundFamily = FontFamily(
    Font(resId = R.font.rushon_ground, weight = FontWeight.Normal)
)

actual val NunitoBlackFamily = FontFamily(
    Font(resId = R.font.nunito_black, weight = FontWeight.Black)
)

actual val NunitoMediumFamily = FontFamily(
    Font(resId = R.font.nunito_medium, weight = FontWeight.Medium)
)
actual val NunitoSemiBoldFamily= FontFamily(
    Font(resId = R.font.nunito_semibold, weight = FontWeight.Medium)
)
actual val NunitoBoldFamily= FontFamily(
    Font(resId = R.font.nunito_bold, weight = FontWeight.Medium)
)
actual val MochiyPopPOne= FontFamily(
    Font(resId = R.font.mochiy_pop_p_one_regular, weight = FontWeight.Medium)
)